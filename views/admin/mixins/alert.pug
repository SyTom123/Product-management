mixin alert-success (time) 
    - if(messages.success)
        div(
            class="alert alert-success"
            show-alert
            data-time= time
        ) #{messages.success} 
            <div close-alert><span>x</span></div>